<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controledehorarios">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DeclaracaoDeInteresseActivity"></activity>
        <activity
            android:name=".activity.MenuActivity"
            android:parentActivityName=".activity.Mostrarausencia" />
        <activity
            android:name=".activity.cadastrarAusencia"
            android:parentActivityName=".activity.cadastrarAusencia" />
        <activity android:name=".activity.Mostrarausencia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>